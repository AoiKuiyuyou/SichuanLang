过场 慢速排序（列表）
    列表长度搁列表嘞长度
    如果列表长度小于等于一
        爬开列表
    就弄个
    慢速排序范围（列表，零，列表长度减一）
    爬开列表
过场多


过场 慢速排序范围（列表，低索引，高索引）
    如果低索引大于等于高索引
        爬开
    就弄个
    中索引搁（低索引加高索引）整除二
    慢速排序范围（列表，低索引，中索引）
    慢速排序范围（列表，中索引加一，高索引）
    如果列表第高索引项小于列表第中索引项
        列表第高索引项，列表第中索引项
        搁
        列表第中索引项，列表第高索引项
    就弄个
    慢速排序范围（列表，低索引，高索引减一）
过场多
