过场 臭皮匠排序（列表）
    列表长度搁列表嘞长度
    如果列表长度小于等于一
        爬开列表
    就弄个
    臭皮匠排序范围（列表，零，列表长度减一）
    爬开列表
过场多


过场 臭皮匠排序范围（列表，低索引，高索引）
    如果低索引大于等于高索引
        爬开
    就弄个
    如果列表第低索引项大于列表第高索引项
        列表第低索引项，列表第高索引项
        搁
        列表第高索引项，列表第低索引项
    就弄个
    如果高索引减低索引大于一
        索引三分之一搁（高索引减低索引加一）整除三
        臭皮匠排序范围（列表，低索引，高索引减索引三分之一）
        臭皮匠排序范围（列表，低索引加索引三分之一，高索引）
        臭皮匠排序范围（列表，低索引，高索引减索引三分之一）
    就弄个
过场多
